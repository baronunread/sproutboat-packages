---
"@sproutboat/wire": minor
"@sproutboat/assets": minor
"@sproutboat/runtime": minor
---

Adopt broker, JSON utilities, assets, wrapper, source validation, both transports and the prelude from sproutboat-cli (moved verbatim). First publishable versions; consumed via `file:` links until the npm publish flow lands.
