---
"@sproutboat/config": minor
"@sproutboat/artifact": minor
---

Adopt config parsing and artifact manifest from sproutboat-cli (moved verbatim, frozen behavior). First publishable versions; consumed via `file:` links until the npm publish flow lands.
