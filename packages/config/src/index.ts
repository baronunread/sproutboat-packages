// @sproutboat/config: sproutboat.jsonc parsing, validation, binding slots.
//
// Moved verbatim from sproutboat-cli/src/config.ts; the platform consumed
// it through the CLI package and now imports it here. Frozen behavior:
// changes go through changesets with both consumers updated.
export * from "./config";
