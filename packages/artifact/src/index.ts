// @sproutboat/artifact: artifact manifest schema and bindings report.
//
// Surface reserved. The implementation moves here from
// sproutboat-cli/src/manifest.ts and src/report.ts (and the platform's
// copy) under the adoption issues linked in the root README. The schema
// is frozen at v2 across this move: additive only, never repurposed.
// Until then this module intentionally exports nothing.
export {};
